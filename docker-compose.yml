services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: capterra
      POSTGRES_PASSWORD: capterra
      POSTGRES_DB: capterra
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
